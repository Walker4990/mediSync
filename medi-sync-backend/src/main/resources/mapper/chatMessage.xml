<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mediSync.project.chat.mapper.ChatMessageMapper">

    <insert id="insertMessage" parameterType="chat">
        INSERT INTO chat_message (sender_id, receiver_id, content, sent_at, chat_type)
        VALUES (#{senderId}, #{receiverId}, #{content}, NOW(), #{chatType})
    </insert>

    <select id="getMessagesBetween" resultType="chat">
        SELECT *
        FROM chat_message
        WHERE (sender_id = #{senderId} AND receiver_id = #{receiverId})
        OR (sender_id = #{receiverId} AND receiver_id = #{senderId})
        ORDER BY sent_at ASC
    </select>

</mapper>

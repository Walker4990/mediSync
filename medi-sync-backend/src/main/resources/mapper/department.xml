<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mediSync.project.medical.mapper.DepartmentMapper">

    <!-- 부서 목록 조회 -->
    <select id="deptSelectAll" resultType="dept">
        SELECT dept_id, dept_name, description, consult_fee, insurance_rate, created_at FROM department
    </select>

</mapper>

<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mediSync.project.medical.mapper.DoctorMapper">

    <!-- 의사 목록 조회 -->
    <select id="doctorSelectAll" resultType="doctor">
        SELECT *
        FROM admin_account acc JOIN department dept ON acc.dept_id = dept.dept_id
        WHERE acc.position = 'DOCTOR'
        ORDER BY acc.admin_id DESC
    </select>

    <!-- 의사 검색 -->
    <select id="doctorSelectById" parameterType="Long" resultType="doctor">
        SELECT * FROM admin_account WHERE admin_id = #{adminId}
    </select>
    <!-- 의사 토글검색 -->
   <select id="doctorSelectByDepartment" parameterType="String">
       select * from admin_account WHERE admin_id = #{adminId}
   </select>

    <!-- 의사 정보 등록 -->
    <insert id="insertDoctor" parameterType="doctor" useGeneratedKeys="true" keyProperty="doctorId">
        INSERT INTO doctor
        (doctor_name, dept_id, license_no, phone, created_at)
        VALUES
        (#{doctorName}, #{deptId}, #{licenseNo}, #{phone}, NOW())
    </insert>

    <!-- 의사 정보 수정 -->
    <update id="editDoctor" parameterType="doctor">
        UPDATE doctor SET doctor_name= #{doctorName}, dept_id= #{deptId}, phone= #{phone}
        WHERE doctor_id= #{doctorId}
    </update>

    <!-- 의사 정보 삭제 -->
    <delete id="delDoctor" parameterType="Long">
        DELETE from doctor WHERE doctor_id= #{doctorId}
    </delete>

    <!--  기본 진료비  -->
    <select id="getConsultFeeByDoctorId" parameterType="long" resultType="map">
        SELECT d.consult_fee AS consultFee, d.insurance_rate AS insuranceRate
        FROM doctor a
        JOIN department d ON a.dept_id = d.dept_id
        WHERE a.doctor_id = #{doctorId}
    </select>
    <select id="findDepartmentByDoctorId" resultType="string">
        SELECT dept.dept_name FROM doctor d
        JOIN department dept ON d.dept_id = dept.dept_id
        WHERE doctor_id = #{doctorId}
    </select>
</mapper>
